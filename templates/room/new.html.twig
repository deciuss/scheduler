{% extends 'base.html.twig' %}

{% block title %}New Room{% endblock %}

{% block body %}

    <h3 class="h3">New Room</h3>
    <p class="lead my-3">
        Create new Room for plan {{ room.plan }}
    </p>

    <div class="my-3">
        <a class="btn btn-primary" href="{{ path('room_index', {'plan': room.plan.id}) }}">Back To List</a>
    </div>

    {{ include('room/_form.html.twig') }}

{% endblock %}
