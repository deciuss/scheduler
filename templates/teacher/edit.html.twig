{% extends 'base.html.twig' %}

{% block title %}Edit Teacher{% endblock %}

{% block body %}

    <h1 class="h1">Edit Teacher</h1>
    <p class="lead my-3">
        Edit student group {{ teacher }}
    </p>

    <div class="my-3">
        <a class="btn btn-primary" href="{{ path('teacher_index', {'plan': teacher.plan.id}) }}">Back To List</a>
    </div>

    {{ include('teacher/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('teacher/_delete_form.html.twig') }}

{% endblock %}
