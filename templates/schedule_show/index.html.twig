{% extends 'base.html.twig' %}

{% block title %}Hello ScheduleShowController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{#<div class="example-wrapper">#}
    <h1>Schedule! ✅</h3>

    <table id="main_table">
        {% for row in table %}
            <tr>
            {% for col in row %}
                <td style="background-color: {{ calculateColor(col) }}">{{ col | raw}}</td>
            {% endfor %}
            </tr>
        {% endfor %}
    </table>

{% endblock %}
