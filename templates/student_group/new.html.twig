{% extends 'base.html.twig' %}

{% block title %}New Student Group{% endblock %}

{% block body %}

    <h3 class="h3">New Student Group</h3>
    <p class="lead my-3">
        Create new Student Group for plan {{ student_group.plan }}
    </p>

    <div class="my-3">
        <a class="btn btn-primary" href="{{ path('student_group_index', {'plan': student_group.plan.id}) }}">Back To List</a>
    </div>

    {{ include('student_group/_form.html.twig') }}

{% endblock %}
