{% extends 'base.html.twig' %}

{% block title %}Edit StudentGroup{% endblock %}

{% block body %}
    <h1>Edit StudentGroup</h1>

    {{ include('student_group/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('student_group_index', {'plan': student_group.plan.id}) }}">back to list</a>

    {{ include('student_group/_delete_form.html.twig') }}
{% endblock %}
