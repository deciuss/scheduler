<form method="post" action="{{ path('student_group_delete', {'id': student_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ student_group.id) }}">
    <button class="btn btn-danger float-right my-3">Delete</button>
</form>
