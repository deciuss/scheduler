<form method="post" action="{{ path('subject_delete', {'id': subject.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subject.id) }}">
    <button class="btn btn-danger float-right my-3">Delete</button>
</form>
