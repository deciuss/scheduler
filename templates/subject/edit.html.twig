{% extends 'base.html.twig' %}

{% block title %}Edit Subject{% endblock %}

{% block body %}
    <h1>Edit Subject</h1>

    {{ include('subject/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary float-right mr-2" href="{{ path('subject_index', {'plan': subject.plan.id}) }}">Back to list</a>

    {{ include('subject/_delete_form.html.twig') }}
{% endblock %}
